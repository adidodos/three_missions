name: three_missions
description: "운동 크루 주간 미션 인증 앱"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_storage: ^13.0.6

  # Auth
  google_sign_in: ^7.2.0

  # State & Routing
  flutter_riverpod: ^3.2.1
  go_router: ^17.1.0

  # Image
  image_picker: ^1.1.2
  flutter_image_compress: ^2.3.0

  # Utils
  intl: ^0.20.2
  share_plus: ^10.1.4
  cached_network_image: ^3.4.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
